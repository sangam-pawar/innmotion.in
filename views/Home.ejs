<!DOCTYPE html>
<html lang="en">
    <%- include('./partials/header.ejs') %>
    
<body class="" style="background-color: blueviolet ;">
    <%- include('./partials/navbar.ejs' , {isAuthenticated:isAuthenticated , name:locals.name})  %>

  

    <div class="container text-white " style=" margin-top: 5rem;
    justify-content: center;
    text-align: center;
    align-items: center;
" >
        <div class="row text-center " style="">
            <div class="col-md-12 d-flex text-center" style="flex-direction: column; align-items: center;">
                <img src="innmotion.png" alt="" srcset="" style="max-width: 15rem; object-fit: scale-down;" >
                <p class="title" style="font-family: 'Righteous', cursive; font-size: 7rem ; ">INNMOTION</p>
                <p class="p-0 text-dark" style="font-size: 1.5rem; font-family: 'Righteous', cursive; ">Every Motion Is Possible With</p>
           
            <% if (isAuthenticated) { %>
                <a href="/Dashboard" class="btn btn-light mt-2 px-5 shadow " style=" font-size: 2rem;
                border: none;
                border-radius: 2rem;
                font-family: 'Righteous', cursive;
                font-family: 'Teko', sans-serif; width: 100%;"  > Let's Move</a>
            <% } else { %>
            
           <a href="/login" class="btn btn-light mt-2 px-5 shadow " style=" font-size: 2rem;
            border: none;
            border-radius: 2rem;
            font-family: 'Righteous', cursive;
            font-family: 'Teko', sans-serif; " > Let's Move</a>
            <% } %>
            </div>
        </div>
    </div>





 <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js" integrity="sha384-IQsoLXl5PILFhosVNubq5LC7Qb9DXgDA9i+tQ8Zj3iwWAwPtgFTxbJ8NT4GN1R8p" crossorigin="anonymous"></script>
 <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.min.js" integrity="sha384-cVKIPhGWiC2Al4u+LWgxfKTRIcfu0JTxR+EQDz/bgldoEyl4H0zUF0QKbrJ0EcQF" crossorigin="anonymous"></script>

<script>
    
    // Check the viewport size
    var mq = window.matchMedia('(max-width: 600px)');

    // Function to handle changes to the viewport size
    function handleViewportChange(mq) {
      if (mq.matches) {
        // If viewport is less than 600px wide, add the new-class
        document.querySelector('.title').style.fontSize ='3.5rem';
 


      } else {
        // If viewport is greater than 600px wide, remove the new-class
        document.querySelector('.title').style.fontSize ='7rem';
  

      }
    }

    // Call the function on load and when the viewport size changes
    handleViewportChange(mq);
    mq.addListener(handleViewportChange);
</script>

</body>
</html>